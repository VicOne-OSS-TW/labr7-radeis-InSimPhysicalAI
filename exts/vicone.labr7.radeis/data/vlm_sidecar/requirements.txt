# Radeis VLM sidecar — install into the `gemma` conda env (or a fresh venv).
# torch/transformers are assumed present in the `gemma` env (torch 2.11+cu128,
# transformers 5.5.4). Pinned loosely; the sidecar only needs eager attention.
torch>=2.4
torchvision>=0.19
transformers>=5.5
accelerate>=0.30
safetensors>=0.4
scipy>=1.10
numpy>=1.24
pillow>=10.0
matplotlib>=3.7
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
huggingface_hub>=0.23
nvidia-ml-py>=12.0
psutil>=5.9
